# CLAUDE.md — disclean（ディスクリン）

このリポジトリで作業するときの決めごと。`~/Sync/CLAUDE.md`（全プロジェクト共通）に上乗せする。
デザインの数値・トークンの唯一の出典は `docs/design-system.md`（HEAVY CANDY）。ここには**判断のしかた**を書く。

---

## 0. このプロジェクトの一行

macOS のディスクを、**消す前に重さで見せて**片づけるツール。CLI（`disclean`）と GUI（`Disclean.app`）が
同じ `DiscleanKit` を共有し、同じ隔離庫・同じ記録を読み書きする。

---

## 1. 常にポップであること（このリポジトリの最優先ルール）

**設計も実装も、常にポップな UI/UX を意識する。** ポップさは飾りではなく、このプロダクトの機能である。
ポップには 2 つの意味があり、**両方を同時に満たす**こと。片方だけでは不合格とする。

### 1.1 見た目がポップ（ビジュアル）

- ベタ塗り・3px の黒キーライン・ぼかし 0 のハードオフセット影。角丸は 22 / 999 / 14 の 3 つだけ。
- 塊は重いグミとして振る舞う。押せば潰れ、離せば行き過ぎて揺れて戻る（`Motion.gummy` / `--gummy`）。
- 色・形・動きの具体値は `docs/design-system.md` に従う。**View に色や寸法のリテラルを直接書かない**
  （GUI は `Tokens`、LP は `:root` のカスタムプロパティを通す）。
- 禁則（グラデーション・ぼかし影・マスコット・紙吹雪の成功演出など）は design-system §1.1 のとおり。

### 1.2 誰にでもわかるのがポップ（わかりやすさ）

見た目が跳ねていても、意味が伝わらなければポップではない。次を守る。

- **画面の言葉は動詞のひらがなを主に置く**。「しらべています」「うつしています」「けしています」。
  専門語（quarantine / Tier / run ID）は、初出でやさしい言い換えを先に書き、原語を括弧で添える。
- **いま何が起きているかを、常に画面に書く**。「処理中」だけの表示を作らない。
  何を・どこまで・あと何件かを出す（§2）。
- **これから何が起きるかを、実行前に書く**。「消える」のか「移るだけ」なのか、戻せるのか、
  いつまで戻せるのかを、押す前に読める場所に置く。
- **起きたことを、終わったあとに書く**。件数・量・見送った理由（サイレント失敗ゼロ）。
  「失敗しました」だけで終えない。何をすれば直るかまで書く。
- **数字には必ず単位と意味を添える**。「12」ではなく「12 / 48 件（25%）」。
- **色だけで意味を伝えない**。Tier のチップには必ず文字（`A` / `B` / `見るだけ`）を併記する。

### 1.3 ポップにしない場所（例外）

安全に関わる情報 —— **何を失うか・取り消せるか・いつ消えるか** —— は装飾しない。
本文サイズ・地の色・最高コントラストで、平文のまま書く。ここだけは跳ねさせない（design-system §1.1）。

---

## 2. 待たせるときの決まり（グルグルを作らない）

**マウスカーソルが回っているだけの時間を作らない。** 待ちが発生する処理はすべて、次の 3 つを通す。

| 層 | もの | 役割 |
|---|---|---|
| Kit | `WorkProgress` / `WorkProgressHandler` | 「いま何を・何件目まで」を 1 件ずつ流す唯一の型 |
| GUI | `BusyState` → `BusyBoard` | 動詞の見出し・ゲージ・いま触っているもの・終わったものの積み上げ・安心の一行 |
| CLI | `ProgressLine` | 同じ内容を端末の 1 行に書き換えて出す（stderr、TTY のときだけ） |

守ること。

- **時間のかかる処理を `Executor` / `QuarantineStore` / `Scanner` に足したら、`onProgress` を流す**。
  1 件ずつ進むループがあるなら、1 件ずつ報せを出す（まとめて 1 回にしない）。
- **飛ばした件・失敗した件も必ず数える**（`Tally.finish` を通す）。数が合わないゲージは嘘になる。
- **GUI の重い処理は `AppModel.work(_:coversScreen:)` を通す**。メインスレッドで回すと画面が固まり、
  そのままカーソルが回るだけの状態に戻ってしまう。
- **総数が分からないうちは割合を作らない**。`fraction` は nil のままにし、「数えています」と正直に書く。
- 新しい待ち画面を各画面で発明しない。板（`BusyBoard`）を使う。置き場所だけを変える
  （その場に出す／画面を覆う = `coversScreen`）。

---

## 3. 安全側の決まり（機能として崩さないもの）

- 破壊的操作は `Executor` と `QuarantineStore.purge` だけを通る。他の場所に削除を書かない。
- 記録できない削除を作らない（監査ログに書けてから動かす）。
- 隔離庫を経由し、既定 7 日は元に戻せる。戻せない操作は、実行前に「取り消せません」と明示する。
- 「対象なし」「権限不足」「ツール未検出」は成功に数えず、理由付きで報告する。

---

## 4. 確認のしかた（ここまでやって 1 タスク）

- `swift build` / `swift test` / `acceptance/run-all.sh`（受入テストは一時ディレクトリに閉じて実ユーザーに触れない）。
- **GUI を変えたら、実際に起動して目で見る**。`tools/make-app.sh --preview` で画面確認用ビルドを作り、
  `DISCLEAN_PREVIEW` で開きたい画面を指定する。`HOME` を一時ディレクトリに差し替えれば、
  実ユーザーのファイルに触れずに本番と同じ経路を通せる（`acceptance/lib.sh` と同じやり方）。
- **LP を変えたら、ブラウザで開いて見る**。レイアウト崩れ・コンソールエラー・`prefers-reduced-motion` を確認する。
- 書き出した画像・スクリーンショットは必ず開いて目視する。寸法とファイルサイズの確認は目視の代わりにならない。
